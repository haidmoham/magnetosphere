[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn \"app:create_app()\" --bind 0.0.0.0:$PORT --worker-class gthread --workers 2 --threads 8 --timeout 60 --preload"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
